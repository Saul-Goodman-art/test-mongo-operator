apiVersion: v1
kind: Service
metadata:
  name: {{ include "orders-app.fullname" . }}-kafka
  labels:
    app: orders-kafka
spec:
  type: ClusterIP
  ports:
    - port: 9092
      targetPort: 9092
      protocol: TCP
      name: kafka
  selector:
    app: orders-kafka
