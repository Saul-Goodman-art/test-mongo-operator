apiVersion: v1
kind: Service
metadata:
  name: {{ include "orders-app.fullname" . }}-logger
  labels:
    app: orders-logger
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: orders-logger
